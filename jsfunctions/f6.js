/* arrow function
one can write any logic 
without return in one line and 
with return in multiple lines 
*/
const great =(a, b) => {
    return a + b;
};
console.log(great(5, 10)); // Outputs: 15
const sum = (a, b) => a + b;
console.log(sum(3, 4)); // Outputs: 7